# 允许外部访问（EKS 环境下，Pod 间通信无需绑定 0.0.0.0，但生产建议限制访问）
bind 0.0.0.0
# 设置 Redis 密码（必填，避免未授权访问）
requirepass ${REDIS_PASSWORD}  # 密码从 K8s Secret 注入
# 开启 RDB 持久化（按需调整）
save 900 1
save 300 10
save 60 10000
dbfilename dump.rdb
dir /data
# 限制最大内存（根据 EKS 节点资源调整）
maxmemory 1gb
maxmemory-policy allkeys-lru